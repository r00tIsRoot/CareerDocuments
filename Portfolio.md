# 안드로이드 개발자 이재원 포트폴리오 (2026)

> "경험으로 비즈니스의 안정성을 설계하고, 사용자의 기대를 기술로 현실화합니다."
> 스타트업부터 중견기업까지, 하드웨어 제어부터 대규모 서비스 운영, 그리고 최신 On-Device AI 구현까지 폭넓은 스펙트럼을 보유한 안드로이드 개발자입니다.

---

## 핵심 역량 (Core Competencies)

- Architecture: MVI, MVVM, 멀티모듈(Multi-module) 기반의 클린 아키텍처 설계 및 구축.
- Modern Android: Jetpack Compose, Coroutines, Flow, Hilt를 활용한 고성능 UI/비즈니스 로직 구현.
- On-Device AI: LLM(LiteRT) 기반 추론 파이프라인 설계, 최적화, 멀티모달 입력 처리.
- Blockchain/Security: MPC 서명 권한 분할 구조의 클라이언트 구현, 키 지분 보관·복구 라이프사이클 관리, 멀티체인 키 파생·서명(Trust Wallet Core/Web3j), Solidity·Hardhat, WalletConnect v2(Reown).
- Stability: Firebase Crashlytics 기반의 정밀 트러블슈팅, Crash-free 99.9% 유지.
- Specialty: 하이브리드 앱 브리지(Bridge) 표준화, IoT 풀스택(HW/FW/Server) 구현, On-Device AI 최적화, 블록체인 지갑 보안 구조 구현, P2E 서비스 운영.

---

## 주요 기술적 성과 (Key Technical Achievements)

### 1. 온체인 신사업 기술 검증 — MPC 지갑 · 예측시장 · 추첨 스마트 컨트랙트
- 배경: 사내 선례가 없는 상태에서, 자산을 안전하게 보관·서명할 지갑이 성립하는지와 사업화 후보였던 두 형태(예측시장 / 추첨)가 온체인에서 동작하는지를 모두 검증해야 하는 과제.
- 수행:
    - MPC 지갑(BlackWallet): 계열사(옥텟)의 MPC 프로세스를 따라 서명 권한을 사용자 기기(`userShare`)·서버(`serverShare`)·옥텟(`octetShare`)이 3자 분할 보유하는 구조를 앱 측에 구현. 사용자 지분은 Android KeyStore 기반 암호화 저장소에 사용자별로 분리 보관하고, 다계정 사용 시 지분이 덮어써지지 않도록 동적 키 저장 방식으로 전환. WalletConnect v2(Reown)를 연동해 외부 dApp의 서명·전송 요청을 사용자 승인 후 중계하는 프록시 지갑 구조까지 확인.
    - 예측시장(SC): AMM 기반 예측시장 컨트랙트(Solidity)를 작성하고, 바이트코드 조립 -> 서명 API -> 브로드캐스트로 개인키 없이 배포하는 Hardhat/Node.js 자동화를 구성. 앱에서는 Web3j로 온체인 조회·베팅·정산을 연동.
    - 추첨(SC): EIP-1167 + CREATE2 Clone Factory와 VRF 오라클(Orakl)을 Kaia에 실연동하여, 검증된 로직을 1회만 배포하고 사용자별 경량 Clone을 복제하는 구조로 설계.
- 결과: **어느 한 주체도 단독으로는 서명이 성립하지 않는 지갑 구조**를 앱까지 관통해 완성하고, `마켓 생성 -> 베팅 -> 정산 -> 보상 청구`와 `회차 생성 -> 참여 -> 추첨 -> 정산 -> 수령`을 각각 온체인 E2E로 완주하여 두 사업 형태의 기술적 타당성과 외부 dApp 연동 경로까지 확보.

### 2. 스포츠 예측 시장 파일럿 앱 '폴리볼'의 AI 데이터 추출 인프라 구축
- 배경: 기존 서버에서만 수행하던 크롤링 작업을 사용자 기기로 분산 처리하여 서버 부하를 낮추고, 온디바이스 AI를 도입해 웹 페이지 구조 변경에 따른 크롤링 로직의 수정 주기를 획기적으로 개선해야 하는 과제.
- 수행: 
    - 파이프라인 설계: WebView $\rightarrow$ 전처리 $\rightarrow$ On-Device LLM $\rightarrow$ 결과로 이어지는 동적 데이터 추출 파이프라인 구현.
    - 저사양 기기 최적화: 입력 데이터 전처리 과정을 최적화하여 리소스 제약 환경에서도 안정적인 추론 속도와 시스템 안정성을 확보.
- 결과: 서버 집중형 크롤링 작업을 다수의 사용자 기기로 분산 처리함으로써 서버 부하를 획기적으로 낮추고, AI 기반 추출을 통해 페이지 구조 변경 시에도 로직 수정이 필요 없는 유연한 아키텍처를 입증.

### 3. 전사적 앱 크래시 75% 감소 및 안정성 혁신
- 배경: '발로소득' 서비스 초기, 개발자가 제어할 수 없는 시점에서 발생하는 Service Notification 관련 오류가 전체 크래시의 75%를 차지.
- 수행: 
    - Notification 등록 시점과 업데이트 시점의 CustomLayout 적용 로직을 분리하는 발상의 전환.
    - 시스템 리소스 부족 시 발생하는 갱신 오류를 원천 차단하기 위해 초기 등록은 시스템 기본 포맷으로, 이후 업데이트만 커스텀으로 처리하도록 구조 개선.
- 결과: 전체 크래시 발생률 75% 감소, 서비스 안정성 99.9% 달성. '2023 올해를 빛낸 앱 최우수상' 수상의 기술적 기반 마련.

### 4. 미디어 로딩 속도 60배 향상 (앨범 로더 최적화)
- 배경: 수천 장의 사진을 보유한 유저 기기에서 앨범 선택 화면 진입 시 심각한 프레임 저하 및 로딩 지연 발생.
- 수행: 
    - 기존의 비효율적인 리스트 로드 방식을 CursorLoader 및 미디어 스토어 쿼리 최적화로 변경.
    - 썸네일 캐싱 전략(Glide) 재설계 및 불필요한 메타데이터 로드 제거.
- 결과: 로딩 속도 60배 이상 향상. 대용량 미디어 환경에서도 끊김 없는 사용자 경험 제공.

---

## 프로젝트 상세 (Project Deep Dives)

### [Personal Project] LMBridge (On-Device LLM SDK)
*2026.04 ~ 현재 / Android용 온디바이스 LLM 브리지 라이브러리*
- 목표: 안드로이드 개발자가 하드웨어 가속 엔진의 복잡성 없이 LLM을 쉽게 구현할 수 있도록 추상화 레이어 제공.
- 구현 성과: 
    - HW 가속 추상화: NPU, GPU, CPU 백엔드를 선택적으로 사용할 수 있는 인터페이스 설계.
    - 멀티모달 파이프라인: 텍스트뿐 아니라 이미지, 오디오 데이터를 처리하는 멀티모달 추론 인터페이스 구현.
    - 모델 생명주기 관리: HuggingFace 연동을 통한 모델 다운로드, 이어받기(Resume), 버전 관리 기능 구현.

### [헥토이노베이션] BlackWallet (MPC 기반 논커스터디 지갑)
*2026.05 ~ 현재 / 안드로이드 책임 
*Kotlin · Compose · Orbit MVI · MPC · Trust Wallet Core · Web3j · Solidity*
- MPC 서명 권한 3자 분할: 계열사(옥텟)의 MPC 프로세스를 따라 사용자 기기(userShare)·서버(serverShare)·옥텟(octetShare)이 서명 권한을 분할 보유하는 구조를 앱 측에 구현.
- 키 지분 관리: 소셜 로그인 -> MPC 키 생성 -> 지분 수신 -> 지갑 생성 온보딩 파이프라인 및 암호화 저장소(Android KeyStore + Tink AEAD) 보관 구현. 다계정 대응을 위해 사용자별 동적 키 저장으로 전환하고 온보딩 분기 매트릭스를 정의.
- 멀티체인 지갑 기능: Trust Wallet Core 기반 EVM/Solana/Bitcoin 키 파생·서명, Web3j 기반 트랜잭션 전송, 잔액·거래내역·출금 구현.
- 온체인 신사업 PoC: 사업화 후보 두 형태인 예측시장(AMM)과 추첨(Clone Factory + Orakl VRF) 컨트랙트를 작성·배포해 온체인 E2E로 검증. 추가로 WalletConnect 프록시 지갑으로 외부 dApp 연동 경로를 검토.
- 멀티디바이스 R&D: TOPRF 기반 시드리스 온보딩을 분석해, 지분을 기기 간 전송하지 않고 `소셜 재인증 + 사용자만 아는 비밀`로 재파생하는 복구 모델을 제안.

### [헥토이노베이션] 폴리볼
*2026.04 ~ 현재 / 안드로이드 책임*
- 하이브리드 앱 아키텍처: 모든 화면을 WebView 기반으로 구성하여 웹-네이티브 간 인터페이스 표준화 및 통신 규격 정의.
- 네이티브-웹 연동 구현: JavaScript Interface를 통한 브리지 레이어 설계 및 App Links 기반의 딥링크 라우팅 로직 구현.
- 온디바이스 AI 파이프라인: WebView $\rightarrow$ 전처리 $\rightarrow$ On-Device LLM $\rightarrow$ 결과로 이어지는 데이터 추출 프로세스를 구축하여 서버 부하를 낮추고 운영 효율성을 극대화.

### [헥토이노베이션] 라운드 (rround)
*2025.01 ~ 2026.03 / 안드로이드 책임*
- 아키텍처 기반 구축: MVI + 멀티모듈 구조를 도입해 빌드 속도를 개선하고 팀 간 협업 충돌을 최소화.
- Interactive UI: Jetpack Compose로 복잡한 상태를 가진 출석체크 바텀시트, 숏츠형 피드 UI 등을 선언형으로 구현.
- 하이브리드 인프라: 웹뷰와 네이티브 간의 통신 규격을 인터페이스화 및 표준화하여 보안과 생산성 동시 확보.
- 범용 라우팅 엔진: 앱 내 모든 진입점에 대응하고 확장 가능한 딥링크 라우팅 프로세스를 설계하여 서비스 접근성을 극대화하고 운영 효율성을 획기적으로 개선.
- 커뮤니티 엔진: WebSocket 기반의 실시간 채팅 서비스 유지보수 및 대규모 피드 시스템 구축.
- GPS 챌린지: 백그라운드 위치 추적 및 GPS 데이터 정밀도 필터링 로직 구현.

### [헥토이노베이션] 발로소득
*2023.04 ~ 2024.12 / 안드로이드 선임*
- UI 현대화 및 Compose 전환: 기존 XML 기반의 핵심 지면(홈 챌린지 리스트, 쇼핑 탭 등)을 Jetpack Compose로 마이그레이션하여 UI 생산성 및 유지보수성 개선.
- 수익 모델 통합: 비디오 광고 및 다양한 오퍼월(애드팝콘, 버즈빌 등)을 서비스 지면에 최적화하여 통합.
- Android 14 대응: targetSDK 34 전환 및 포그라운드 서비스 권한 정책 변경에 따른 시스템 전반의 리팩토링 주도.

### [(주)홍복] IrisCollector (AI 데이터 수집 솔루션)
*2022.06 ~ 2022.08 / 연구개발팀 연구원*
- 데이터 획득: 자사 홍채 촬영 SDK를 어플리케이션에 최적화하여 연동하고, 획득한 데이터를 로컬에 임시 보관 및 전송하는 프로세스 구현.
- AI 진단 결과 제공: 자사 홍채 분석 AI 모델을 활용하여 촬영된 홍채 이미지로부터 건강 상태를 진단하고, 이를 사용자에게 시각적으로 전달하는 진단 결과 제공 로직 구현.
- 인터랙티브 UI: 모바일에 최적화된 고해상도 촬영 가이드 라인 및 장기별 진단 결과 시각화 레이아웃 구현으로 현장 사용자 편의성 제공.

### [(주)홍복] EyeOClockW (AI 건강 진단 솔루션)
*2020.01 ~ 2022.08 / 연구개발팀 연구원*
- 특수 하드웨어 연동: USB 카메라 드라이버 연동을 통한 홍채 촬영 제어 및 실시간 고해상도 이미지 전처리 파이프라인 구축.
- 영역 선택 자동화: 안드로이드 앱 아키텍처 및 Product Flavor 구조 최적화를 통해 바이어 요구에 맞춘 수동/자동 영역 선택 기능의 유연한 분기 구현.
- 연동 및 출력 시스템: 진단 결과를 이메일 또는 로컬 프린터(Bluetooth/Serial 통신)를 통해 실시간으로 전송 및 인쇄할 수 있는 하드웨어 연동 라이브러리 개발.
- 운영 안정화: 공공기관 설치 기기의 비정상 상황에 대비하여 로그 분석 및 비정상 종료 시 자동 재시작 메커니즘을 적용하여 안정성 확보.
- 공공기관 납품 및 운영: 동작구청, 동작구 보건소, 여수시청 등 다수의 공공기관에 솔루션을 납품하고 유지보수를 수행하여 실환경 서비스 안정성 검증 및 운영 경험 확보.

### [(주)홍복] HongTools (AI 학습 데이터 라벨링 도구)
*2020.03 ~ 2022.08 / 연구개발팀 연구원*
- 라벨링 UI 개발: 안드로이드 터치 조작으로 홍채 이미지 내 특정 장기 영역을 정밀하게 라벨링(Classification/Segmentation)할 수 있는 그래픽 편집 도구 구현.
- 핵심 인프라 기여: 사내 주요 AI 프로젝트 학습 데이터의 90% 이상을 확보하는 중추적인 데이터 정제 인프라로 활용되어 AI 파이프라인 생산성 극대화.

### [(주)홍복] IoT 스마트 홈 솔루션 (CozyQ & 스마트 도어락)
*2018.06 ~ 2019.12 / 연구개발팀 연구원*
- IoT 통합 앱 개발: Bluetooth Low Energy(BLE) 및 TCP/IP 소켓 통신을 이용한 스마트 도어락, 스마트 소켓 및 온습도 센서 모니터링/제어 앱 독자 전담 개발.
- 지오펜스(Geofencing) 연동: 안드로이드 위치 기반 API를 사용하여 사용자가 집 주변에 근접하면 도어락이 자동 해제되거나 스마트홈 제어가 연동되는 공간 감지 트리거 구현.
- IoT 풀스택 참여: 온습도/미세먼지 스마트 센서 디바이스의 회로 설계 및 아두이노(C/C++) 기반 펌웨어 개발, Node.js 기반의 mobidus IoT 서버 구축 및 MySQL 데이터베이스 설계 주도.
- TIPS 사업 성공 완수: 중소벤처기업부 주관 TIPS(민간투자주도형 기술창업지원) 사업의 핵심 개발자로서 시제품 회로/펌웨어/앱 제작 및 실환경 필드 테스트 검증을 이끌어 최종 평가 통과에 공헌.

---

## My Philosophy
저는 단순한 기능 구현을 넘어, "유지보수가 즐거운 코드"와 "사용자가 신뢰할 수 있는 안정성"을 지향합니다. 홍채 인식 기반의 초기 AI 데이터 수집부터 온디바이스 LLM 프레임워크 설계, 그리고 자산을 다루는 지갑의 신뢰 구조 설계까지, 기술의 진화 흐름에 맞춰 안드로이드 플랫폼에서의 최적의 구현 방식을 끊임없이 탐구해 왔습니다. 이러한 도전 정신으로 팀의 기술 성장을 견인하고 비즈니스의 성공을 뒷받침하겠습니다.
